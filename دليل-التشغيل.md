# دليل تشغيل نظام متابعة الأدوار — عيادة الأسنان

## ١) إنشاء مشروع Firebase (مجاني)

1. ادخل على `console.firebase.google.com` → **Add project** → اكتب اسم مثل `islah-queue` → تجاهل Google Analytics → Create.
2. من القائمة اليسرى: **Build → Realtime Database** → Create Database → اختر أقرب سيرفر (europe-west1) → ابدأ بـ **Test mode** مؤقتاً.
3. من القائمة: **Build → Authentication** → Get started → فعّل **Email/Password**.
4. في نفس صفحة Authentication → تبويب **Users** → **Add user**:
   - Email: `dental@islah.clinic`
   - Password: كلمة سر قوية
   - ده حساب العيادة الثابت اللي أي ممرض هيدخل بيه.

## ٢) نسخ بيانات الربط

- اضغط على ⚙️ **Project settings** → انزل لـ **Your apps** → أيقونة الويب `</>` → سجّل اسم أي حاجة → هيظهرلك بلوك `firebaseConfig`.
- افتح `clinic-queue.html` وابحث عن السطر المكتوب فيه:

```js
const firebaseConfig = {
  apiKey:      "PASTE_API_KEY",
  ...
};
```

- استبدل القيم كلها بالقيم اللي نسختها. **مهم جداً**: تأكد إن فيه سطر `databaseURL` موجود، لو مش ظاهر في اللي نسخته اكتبه يدوي بالشكل ده:
  `databaseURL: "https://اسم-المشروع-default-rtdb.firebaseio.com"`

## ٣) قواعد الأمان (بعد التجربة)

في Realtime Database → تبويب **Rules** ضع:

```json
{
  "rules": {
    "clinics": {
      ".read": true,
      "$cid": { ".write": "auth != null" }
    },
    "doctors": {
      ".read": true,
      ".write": "auth != null"
    }
  }
}
```

يعني: الشاشات والمرضى **يقرأوا فقط** بدون تسجيل دخول، والتعديل للعيادة المسجّلة بس.

## ٤) الخط والشعار

بجوار ملف `clinic-queue.html` اعمل:

```
clinic-queue.html
logo.png              ← شعار المجمع
fonts/RB-Regular.woff2
fonts/RB-Bold.woff2
```

لو عندك الخط بصيغة `.ttf` بس، حطه باسم `fonts/RB-Regular.ttf` — الكود بيقبل الصيغتين.

## ٥) الروابط الأربعة

الملف واحد، والشاشة بتتغيّر حسب آخر الرابط:

| الشاشة | الرابط |
|---|---|
| التمريض (دخول بالحساب) | `clinic-queue.html` |
| شاشة التلفزيون | `clinic-queue.html#/tv` |
| الاستقبال والإدارة | `clinic-queue.html#/reception` |
| موبايل المريض (QR) | `clinic-queue.html#/patient` |

ارفعه على GitHub Pages زي باقي أدواتك وخلاص.

## ٦) ملاحظة عن الصوت

المتصفحات بتمنع الصوت قبل أول لمسة. على شاشة التلفزيون اضغط زر **ملء الشاشة** مرة واحدة أول ما تفتحها — ده بيفعّل نغمة التنبيه لبقية اليوم.

## ٧) التشغيل اليومي

1. الممرض يفتح الرابط ويسجل دخول بحساب العيادة.
2. يكتب اسم الطبيب + الفترة + مكان العيادة → **بدء جلسة الطبيب** → العداد يرجع ١.
3. يحرّك البكرة أو يضغط +١ مع كل مريض.
4. آخر اليوم: **إنهاء** → الشاشات تكتب "انتهت العيادة" والأرقام تختفي.


